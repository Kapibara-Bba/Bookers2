<div class="body">
  <div class="container">
    <div class="row">
      <div class="col-xs-3">
        <h2>User info</h2>
        <%= attachment_image_tag @user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: 'no_image.jpg', class: "pull-left profile-img" %>
        <table class="table">
          <tboby>
            <tr>
              <th>name</th>
              <th><%= @user.name %></th>
            </tr>
            <tr>
              <th>introduction</th>
              <th><%= @user.introduction %></th>
            </tr>
          </tboby>
        </table>
        <div class="row">
          <%= link_to edit_user_path(@user) do %>
          <button type="button" class="btn btn-default btn-block col-xs-12">
            <i class="glyphicon glyphicon-wrench" aria-hidden="true"></i>
          </button>
          <% end %>
        </div>
        <h2>New book</h2>
        <%= form_with model:@new_book, local: true do |f| %>
        <% if @new_book.errors.any? %>
          <div id="error_explanation">
            <h2><%= pluralize(@new_book.errors.count, "error") %> prohibited this book from being saved:</h2>
            <ul>
              <% @new_book.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        <% end %>
          <div class="field-row">
            <label for="title">Title</label>
            <div class="row">
                <%= f.text_field :title, class: 'col-xs-12' %>
            </div>
          </div>
          <div class="field-row">
            <label for="opinion">Opinion</label>
            <div class="row">
              <%= f.text_area :body, class: 'col-xs-12' %>
            </div>
          </div>
          <div class="actions-row">
            <div class="row">
              <%= f.submit "Create Book", class: 'btn btn-primary col-xs-12' %>
            </div>
          </div>
        <% end %>
        </div>
      <div class="col-xs-9">
        <h2>Book detail</h2>
        <table class="table table-hover table-inverse">
          <tbody>
            <tr>
              <td>
                <%= link_to user_path(@user) do %>
                  <%= attachment_image_tag @user, :profile_image, :fill, 40, 40, format: 'jpeg', fallback: "no_image.jpg", size:'40x40' %><br>
                  <%= @user.name %>
                <% end %>
              </td>
              <td>
                <span class="tbody"><%= @book.title %></span>
              </td>
              <td>
                <span class="tbody"><%= @book.body %></span>
              </td>
              <td>
                <%= link_to "Edit", edit_book_path(@book), class: 'btn btn-success' %>
              </td>
              <td>
                <%= link_to "Destroy", book_path(@book), method: :delete, "data-confirm" => "Are you sure?", class: 'btn btn-danger' %>
              </td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
